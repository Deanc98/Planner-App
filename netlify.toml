[build]
  # Netlify will automatically run 'npm run build' which now uses react-scripts
  command = "npm run build"
  # CRITICAL: CRA builds output to a folder named 'build', not 'dist'
  publish = "build" 
